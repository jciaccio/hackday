// create variable for score of current player
var currentScore = 0;

// create variable for name of player. Rename in game
var currentPlayer = _________

// create open array for scores to be added
var allScores = [];

// add scores to array
allScores.push(currentScore += "   " += currentPlayer);

// sort scores high to low
allScores.sort()
allScores.reverse();

// new array for top scores, push top 10 to new array
var topScores = [];
topScores.push(allScores.slice(0, 9));